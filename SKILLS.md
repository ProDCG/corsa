# ANTIGRAVITY SKILLS: RIDGE-LINK ORCHESTRATION

### NETWORK ORCHESTRATION
* **UDP HEARTBEAT:** Broadcast/Listen logic. Port 5001. Discovery of unprovisioned rigs.
* **TCP COMMANDS:** Master-to-Slave instruction set. JSON payloads. `START`, `KILL`, `SYNC`, `REBOOT`.
* **SMB MANAGEMENT:** Windows `net share` automation. Credential-less internal access. /23 subnet logic.
* **STATIC TOPOLOGY:** Manual IP assignment. DNS `1.1.1.1`. DHCP reservation logic.

### BINARY DEPLOYMENT & DEVOPS
* **NUITKA COMPILATION:** Python-to-C++ machine code. `--standalone`. `--onefile`. Anti-tamper builds.
* **INNO SETUP:** `.iss` scripting. Registry persistence. Firewall automation. Windows Startup hooks.
* **CI/CD PIPELINE:** GitHub Actions. Automated Windows Runner builds. Release artifacts distribution.
* **VERSION SYNC:** Admin-as-CDN. Client binary self-replacement logic. Hash-based manifest verification.

### SIMULATION INTEROP (AC/CSP)
* **TELEMETRY:** SimHub API (`:8888`). AC Shared Memory (`ac_physics`). UDP data buffers.
* **CSP LOGIC:** `race.ini` generation. Weather FX/Sol/Pure injection. Dynamic time-of-day offsets.
* **SESSION MANAGEMENT:** `acs.exe` process control. Entry-list placeholder swapping. Hot-joining logic.
* **ROBOCOPY SYNC:** Multi-threaded (`/MT:32`). Mirroring (`/MIR`). Assetto Corsa content skeletonization.

### INFRASTRUCTURE & SECURITY
* **HWID LOCKING:** WMI Baseboard serial + MAC hash. Salted AES. Rig-to-Admin pairing.
* **LICENSE ENFORCEMENT:** Cloud Pulse verification. Signed JWT facility keys. Graceful suspension state.
* **AUDIO STACK:** Murmur (Mumble) server. Dynamic channel creation via GRPC/Ice. URI-based auto-connect.
* **KIOSK MODE:** Win32API focus management. PySide6 full-screen overrides. Process-watchdog.

### DATA & ANALYTICS
* **LEADERBOARD:** SQL `UPSERT`. Session-best filtering. Real-time WebSocket TV overlays.
* **REPLAY ENGINE:** G-Force trigger logic. OBS-WebSocket integration. Local clip hosting.
* **REMOTE TUNING:** Document-path injection. `.ini` setup distribution via Orchestrator.